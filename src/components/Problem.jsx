// Componente que muestra el problema del cliente más un botón para soluciones propias

import "../styles/problem.css"

const Problem = (props) => {
    return (
        <div></div>
    );
}

export default Problem;